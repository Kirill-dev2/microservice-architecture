apiVersion: v1
kind: Service
metadata:
  name: otus-hw03-service
  labels:
    app: simple-service
spec:
  selector:
    app: simple-service
  type: NodePort
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8000